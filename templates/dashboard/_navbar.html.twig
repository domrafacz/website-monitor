<nav class="navbar navbar-expand-lg navbar-bg sticky-md-top">
    <div class="container-fluid">
        <button class="sidebar-toggler border-1" onclick="dashboardSidebarToggle()"><i class="fa-solid fa-bars"></i></button>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0 me-1">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    {{ app.user.userIdentifier }}
                </a>
                <ul class="dropdown-menu position-absolute dropdown-menu-end dropdown-user-nav">
                {% if is_granted('ROLE_ADMIN') %}
                    <li>
                        <a class="dropdown-item text-danger" href="{{ path('app_admin_dashboard') }}">
                            <i class="fa-solid fa-lock"></i>
                            {% trans %}admin_dashboard_title{% endtrans %}
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" href="{{ path('app_dashboard') }}">
                            <i class="fa-solid fa-sliders"></i>
                            {% trans %}user_dashboard_title{% endtrans %}
                        </a>
                    </li>
                {% endif %}
                    <li>
                        <a class="dropdown-item" href="{{ path('app_user_settings') }}">
                            <i class="fa-solid fa-user-pen"></i>
                            {% trans %}settings_link{% endtrans %}
                        </a>
                    </li>
                    <li>
                        <hr class="dropdown-divider">
                    </li>
                    <li>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">
                            <i class="fa-solid fa-arrow-right-from-bracket"></i>
                            {% trans %}logout_link{% endtrans %}
                        </a>
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</nav>